
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Abubakr uchun maxsus: Hamma o'qishi va yozishi mumkin.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
